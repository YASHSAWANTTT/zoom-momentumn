# Zoom App Credentials (from Zoom Marketplace)
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
ZOOM_REDIRECT_URL=https://your-server-domain/api/auth/callback
ZOOM_SECRET_TOKEN=

# Session
SESSION_SECRET=change-me-to-a-random-string

# Database — Neon or Railway PostgreSQL (https://console.neon.tech)
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# AWS Bedrock (Pulse, Arena, Anchor AI, recovery, cues) — IAM user or role with bedrock:InvokeModel
# In AWS Console → Bedrock → Model access, enable the model you use (default below).
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Optional: override default model (must match an enabled model in your region)
# BEDROCK_MODEL_ID=meta.llama3-70b-instruct-v1:0

# RTMS native client (@zoom/rtms) — use same Zoom Marketplace OAuth app as the in-meeting app unless Zoom specifies otherwise
ZM_RTMS_CLIENT=
ZM_RTMS_SECRET=
# Optional: path to CA certificate for TLS to Zoom RTMS servers
# ZM_RTMS_CA=

# Server
PORT=3001
CLIENT_URL=http://localhost:5173
